class node:
	def __init__(self, value):
		self.right = None
		self.left = None
		self.val = value

class Tree:
# if want to build the balance tree, use array, want to build BST use link list. 
	self.head = None
	def create(num):
		for i in range(num, tree):
			ele = node(i)
			self.insert(tree, ele)
	def insert(self, tree, ele):
		x = tree
		while tree.left or tree.right:
			if x <= tree.val:
				tree = tree.left if tree.left else tree.left = node(x)
			if x > tree.val:
				tree = tree.right if tree.right else tree.right = node(x)
		

t = Tree()
t.create(t)
print t


	
