class Solution:
    # @param dungeon, a list of lists of integers
    # @return a integer
    def calculateMinimumHP(self, dungeon):
        for i in range(len(dungeon)):
        	for j in range(len(dungeo[i])):
        		